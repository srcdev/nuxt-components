{
  "component_patterns": {
    "form_components": {
      "base_interface": "BaseCheckboxRadioProps from ~/types/forms/types.forms",
      "common_props": [
        "id: string - unique identifier",
        "name: string - form field name",
        "theme: string - visual theme variant",
        "size: string - component size (small, medium, large)",
        "fieldHasError: boolean - error state indicator",
        "styleClassPassthrough: array - for styling flexibility"
      ],
      "patterns": {
        "model_binding": "Uses defineModel<T>() for v-model support",
        "theme_support": "data-theme attribute with CSS custom properties",
        "error_handling": "Conditional styling based on fieldHasError prop",
        "accessibility": "Proper ARIA attributes and semantic HTML"
      }
    },
    "layout_components": {
      "AccordianCore": {
        "purpose": "Multi-panel accordion container that creates expandable sections",
        "file_location": "app/components/accordian/AccordianCore.vue",
        "props": {
          "name": "string - unique identifier for single-open behavior (optional)",
          "itemCount": "number - number of accordion panels to create (required)",
          "animationDuration": "number - animation duration in milliseconds (default: 300)",
          "styleClassPassthrough": "string|array - classes for styling flexibility"
        },
        "slot_patterns": {
          "summary": "accordian-{index}-summary - header content for each panel",
          "icon": "accordian-{index}-icon - icon content for each panel",
          "content": "accordian-{index}-content - expandable content for each panel"
        },
        "behavior": {
          "without_name": "Multiple panels can be open simultaneously",
          "with_name": "Only one panel open at a time (radio-button behavior)",
          "dependencies": "Uses ExpandingPanelCore component internally"
        },
        "styling_approach": {
          "base_class": ".display-accordian with auto-centering and max-width",
          "item_styling": ".accordian-item class passed to each ExpandingPanel",
          "animation_binding": "CSS custom property v-bind for dynamic timing",
          "customization": "Functional base styles, custom design via HOC style blocks"
        },
        "usage_example": {
          "basic": "AccordianCore with :item-count and slot templates",
          "single_open": "Add :name prop for exclusive panel behavior",
          "custom_timing": ":animation-duration for different transition speeds",
          "demo_location": "app/pages/ui/accordian.vue"
        }
      }
    },
    "composable_patterns": {
      "useStyleClassPassthrough": {
        "purpose": "Dynamic styling class management with reactive updates",
        "file_location": "app/composables/useStyleClassPassthrough.ts",
        "full_api": {
          "elementClasses": "computed reactive class string for template binding",
          "updateElementClasses": "toggle classes on/off dynamically (string|array)",
          "resetElementClasses": "reset to initial prop value, used in watchers",
          "styleClassPassthroughRef": "underlying reactive reference"
        },
        "usage_patterns": {
          "basic": "const { elementClasses } = useStyleClassPassthrough(props.styleClassPassthrough)",
          "dynamic": "const { elementClasses, updateElementClasses } = useStyleClassPassthrough(props.styleClassPassthrough)",
          "with_watcher": "watch(() => props.styleClassPassthrough, () => resetElementClasses(props.styleClassPassthrough))"
        },
        "examples": {
          "conditional_classes": "updateElementClasses(['has-left-button', 'has-right-button'])",
          "slot_based": "updateElementClasses based on slot presence",
          "state_based": "Toggle classes based on component state"
        }
      },
      "useFormControl": {
        "purpose": "Centralized form state and validation management",
        "integrates_with": ["Zod validation", "error display", "form submission"]
      }
    }
  },
  "styling_system": {
    "css_architecture": {
      "methodology": "Custom properties with BEM-like structure",
      "theme_variables": {
        "--theme-input-border": "Dynamic border colors",
        "--theme-input-outline": "Focus outline styling",
        "--theme-checkbox-symbol-color": "Icon colors for checkboxes/radios",
        "--form-element-outline-width": "Consistent focus indicators"
      },
      "responsive_approach": "CSS Grid and Flexbox with container queries where supported"
    }
  },
  "common_tasks": {
    "creating_form_component": {
      "steps": [
        "1. Extend BaseCheckboxRadioProps interface",
        "2. Use defineModel for v-model support",
        "3. Implement useStyleClassPassthrough for styling",
        "4. Add theme data attribute for CSS targeting",
        "5. Include proper ARIA attributes",
        "6. Create comprehensive test suite"
      ]
    },
    "adding_layout_component": {
      "example": "AccordianCore pattern",
      "steps": [
        "1. Define clear prop interface with required/optional properties",
        "2. Use v-for with itemCount for dynamic panel generation",
        "3. Implement slot forwarding with dynamic slot names",
        "4. Apply useStyleClassPassthrough for styling flexibility",
        "5. Use v-bind for CSS custom properties when needed",
        "6. Keep base styles functional, allow HOC customization",
        "7. Create comprehensive test suite with mountSuspended"
      ],
      "slot_naming": "Use descriptive patterns like 'component-{index}-{type}'",
      "dependencies": "Leverage existing components when possible"
    },
    "adding_tests": {
      "setup": "Use mountSuspended from @nuxt/test-utils",
      "patterns": [
        "Test component mounting with various props",
        "Verify v-model binding and reactivity",
        "Check accessibility attributes",
        "Test error states and styling",
        "Validate slot rendering when applicable"
      ]
    }
  }
}
